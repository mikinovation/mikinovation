{
  "name": "mikinovation",
  "scripts": {
    "format": "prettier . --write",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.5",
    "prettier": "3.3.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "lint-staged": {
    "**/*.{md,yml}": [
      "bun format",
      "git add ."
    ]
  }
}
